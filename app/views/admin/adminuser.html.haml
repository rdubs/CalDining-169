%h1 Admin users
- @admin.each do |admin|
    =admin.email
    =button_to 'Make regular user', update_path(admin.id), method: :put
    %br
%br
%h1 Non Admin users
- @nonadmin.each do |nonadmin|
    =nonadmin.email
    =button_to 'Make Admin', update_path(nonadmin.id), method: :put 